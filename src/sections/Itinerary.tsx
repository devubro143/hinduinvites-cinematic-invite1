import { memo } from "react";

/**
 * The itinerary schedule has been successfully integrated into the dynamic and expandable 
 * Ceremony Chapters system in Events.tsx. This section is no longer active in the layout.
 */
export const Itinerary = memo(function Itinerary() {
  return null;
});
